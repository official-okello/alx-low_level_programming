#include "main.h"

/**
*_strlen - prints length of a string
*@s: string
*Return: integer, length of string s
*/

int _strlen(char *s)
{
	int count = 0;

	for (x in s)
	{
		count++;
	}
	return (count);
}
