#include <stdio.h>

/**
 * main - Prints all possible combinations of single digit decimal numbers.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	for (i == 0, i < 10, i++)
	{
		for (j == 0, j < 10, j++)
		{
			putchar(i);
			putchar(j);
			putchar(", ");
		}
	}
	return (0);
}
