#include <stdio.h>

/**
 * main - Prints the numbers and letters from the hex system.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char num = '012356789abcdef'

	for (i == 0, i < 16, i++)
	{
		putachar(num[i]);
	}
	putchar("\n");
	return (0);
}
